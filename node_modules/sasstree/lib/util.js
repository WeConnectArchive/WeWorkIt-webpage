"use strict";

var _interopRequire = function (obj) { return obj && obj.__esModule ? obj["default"] : obj; };

Object.defineProperty(exports, "__esModule", {
  value: true
});

var TOKENS = _interopRequire(require("./Tokens"));

/**
 * Return whether the given character code is for a whitespace character.
 * @param {int} code
 * @returns {boolean}
 */
var isWhitespace = function isWhitespace(code) {
  return code == TOKENS.space || code == TOKENS.newline || code == TOKENS.tab || code == TOKENS.carriageReturn || code == TOKENS.formFeed;
};

exports.isWhitespace = isWhitespace;
/**
 * Return whether the given character code is for a newline character.
 * @param {int} code
 * @returns {boolean}
 */
var isNewline = function isNewline(code) {
  return code == TOKENS.newline || code == TOKENS.carriageReturn;
};

exports.isNewline = isNewline;
/**
 * Return whether the given character code is for a digit [0-9].
 * @param {int} code
 * @returns {boolean}
 */
var isDigit = function isDigit(code) {
  return code >= 48 && code <= 57;
};

exports.isDigit = isDigit;
/**
 * Return whether the given character code is for a letter [A-Za-z].
 * @param {int} code
 * @returns {boolean}
 */
var isLetter = function isLetter(code) {
  return code >= 97 && code <= 122 || code >= 65 && code <= 90;
};
exports.isLetter = isLetter;